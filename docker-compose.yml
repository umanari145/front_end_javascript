version: '3'
services:
  php:
    build:
        context: "./docker/php"
        dockerfile: "Dockerfile"    
    ports:
        - "80:80"
    volumes:
        - .:/var/www/html
    networks:
     - js

networks:
  js:
    driver: "bridge"
